[package]
name = "tpt-cms"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Cryptographic Message Syntax (PKCS#7 / CMS, RFC 5652) — zero-copy, no_std parsing and verification"
readme = "README.md"
keywords = ["cms", "pkcs7", "asn1", "pki", "der"]
categories = ["cryptography", "parser-implementations"]

[dependencies]
tpt-asn1-core = { workspace = true }
tpt-x509 = { workspace = true }

[features]
default = ["std", "alloc"]
std = ["alloc"]
alloc = []
